name: Build and Push

on:
  push:
    branches: ["master"]
    tags: ["v*"]
  pull_request:
    branches: ["master"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to Quay.io
        uses: docker/login-action@v3
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_ENCRYPTED_PASSWORD }}

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push (latest)
        if: ${{ github.ref == 'refs/heads/master' }}
        uses: docker/build-push-action@v6
        with:
          push: true
          tags: |
            quay.io/canh25xp/asciiquarium:latest
            ghcr.io/canh25xp/asciiquarium:latest

      - name: Build and push version tag
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        uses: docker/build-push-action@v6
        with:
          push: true
          tags: |
            quay.io/canh25xp/asciiquarium:${{ github.ref_name }}
            ghcr.io/canh25xp/asciiquarium:${{ github.ref_name }}
